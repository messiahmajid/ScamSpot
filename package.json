{
  "name": "scamspot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "verify": "cd chrome-extension && npm test -- --watchAll=false && npm run build:dist && cd ../backend && npm test"
  }
}
